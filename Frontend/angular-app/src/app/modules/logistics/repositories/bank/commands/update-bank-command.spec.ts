import { UpdateBankCommand } from './update-bank-command';

describe('UpdateBankCommand', () => {
  it('should create an instance', () => {
    expect(new UpdateBankCommand()).toBeTruthy();
  });
});
