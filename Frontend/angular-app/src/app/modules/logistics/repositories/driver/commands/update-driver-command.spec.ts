import { UpdateDriverCommand } from './update-driver-command';

describe('UpdateDriverCommand', () => {
  it('should create an instance', () => {
    expect(new UpdateDriverCommand()).toBeTruthy();
  });
});
