import { GetAllDriversQuery } from './get-all-drivers-query';

describe('GetAllDriversQuery', () => {
  it('should create an instance', () => {
    expect(new GetAllDriversQuery()).toBeTruthy();
  });
});
