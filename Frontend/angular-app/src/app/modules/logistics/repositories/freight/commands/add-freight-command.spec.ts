import { AddFreightCommand } from './add-freight-command';

describe('AddFreightCommand', () => {
  it('should create an instance', () => {
    expect(new AddFreightCommand()).toBeTruthy();
  });
});
