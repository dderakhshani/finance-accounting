import { GetAllUsanceQuery } from './get-all-usance-query';

describe('GetAllUsanceQuery', () => {
  it('should create an instance', () => {
    expect(new GetAllUsanceQuery()).toBeTruthy();
  });
});
