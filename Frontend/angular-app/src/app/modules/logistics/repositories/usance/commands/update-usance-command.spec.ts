import { UpdateUsanceCommand } from './update-usance-command';

describe('UpdateUsanceCommand', () => {
  it('should create an instance', () => {
    expect(new UpdateUsanceCommand()).toBeTruthy();
  });
});
