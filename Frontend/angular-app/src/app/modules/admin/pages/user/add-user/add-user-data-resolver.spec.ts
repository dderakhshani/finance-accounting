import { AddUserDataResolver } from './add-user-data-resolver';

describe('AddUserDataResolver', () => {
  it('should create an instance', () => {
    expect(new AddUserDataResolver(<any>null)).toBeTruthy();
  });
});
